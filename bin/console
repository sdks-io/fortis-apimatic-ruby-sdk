#!/usr/bin/env ruby

# Load the lib folder into Ruby's load path
$LOAD_PATH.unshift(File.expand_path('../lib', __dir__))

# Require the gem
require 'fortis_api'

puts 'FortisApi SDK loaded!'
puts 'You can now create a client with: client = FortisApi::Client.new'
puts 'Or use from_env: client = FortisApi::Client.from_env'

# Start an interactive IRB session
require 'irb'
IRB.start
